import { createClient } from '@libsql/client';

const url = "libsql://defcon1-defconmax.turso.io"; // we can just pull from process.env, but wait, I can just console.log the process.env directly when run with Next.js or tsx.
